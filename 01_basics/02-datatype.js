"use strict"; //treat all js code as newer version of javaScript.

// alert("hello");  it will not work because we are using nodejs not javascript 

// in js or any programming language code redability is very important.


// datatype
    /*
        number ==> range is 2 to power 53
        bigInt
        string => ""
        boolean ==> true/flase
        null => standalone value  (it is a special represation of an empty value)
        undefined => not value assigment
        symbol => unique


        object
        array

    */

        //to check datatype we have typeOf()

    let name = "ashesh";
    let age = 22;
    let isValid = true;
    let temp = null;

    console.table([typeof(name),typeof(age), typeof(isValid),typeof(temp)]);
